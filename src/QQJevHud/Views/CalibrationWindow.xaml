<Window x:Class="QQJevHud.Views.CalibrationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择 QQ 聊天区域" WindowStyle="None" ResizeMode="NoResize" Topmost="True"
        ShowInTaskbar="False" Background="#F8FFFFFF">
  <Grid>
    <Image x:Name="Preview" Stretch="Fill" />
    <Canvas x:Name="SelectionCanvas" Background="Transparent"
            MouseLeftButtonDown="OnMouseDown" MouseMove="OnMouseMove" MouseLeftButtonUp="OnMouseUp">
      <Rectangle x:Name="Selection" Stroke="#FF3B82F6" StrokeThickness="2" Fill="#263B82F6" />
    </Canvas>
    <Border VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,14,0,0" Padding="12,8" CornerRadius="8" Background="#EE1F2329">
      <TextBlock Foreground="White" Text="拖出聊天消息区域；只会在这块区域识别文字" />
    </Border>
    <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Right" Orientation="Horizontal" Margin="0,0,16,16">
      <Button Content="取消" Click="CancelClick" />
      <Button Content="使用此区域" Click="SaveClick" />
    </StackPanel>
  </Grid>
</Window>
