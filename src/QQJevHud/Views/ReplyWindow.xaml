<Window x:Class="QQJevHud.Views.ReplyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="QQ Jev HUD · 候选回复" Height="600" Width="460"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResize"
        Background="#FFF7F8FA" FontFamily="Segoe UI, Microsoft YaHei UI" FontSize="13">
    <Window.Resources>
        <Style x:Key="ToneCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource ToneCard}">
            <StackPanel>
                <TextBlock Text="对方说" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,0,0,3"/>
                <TextBlock x:Name="MessageText" Text="（等待新消息…）" TextWrapping="Wrap"
                           Foreground="{StaticResource PrimaryTextBrush}" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="重新生成" Click="RegenerateClick"/>
            <TextBlock x:Name="StatusLabel" Text="" Foreground="{StaticResource SecondaryTextBrush}"
                       VerticalAlignment="Center" TextWrapping="Wrap"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="CandidatesHost">
                <TextBlock Text="还没有候选。收到新消息后会自动生成；也可点「重新生成」。"
                           Foreground="{StaticResource SecondaryTextBrush}" TextWrapping="Wrap"/>
            </StackPanel>
        </ScrollViewer>

        <TextBlock Grid.Row="3" Margin="0,8,0,0"
                   Text="发送由你决定。「填入」会把回复粘进 QQ 输入框，确认后再点发送。"
                   Foreground="{StaticResource SecondaryTextBrush}" TextWrapping="Wrap"/>
    </Grid>
</Window>
